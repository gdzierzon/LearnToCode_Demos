for(let i = 0; i < 10; i++) {
    // i controlls the OUTER loop
    let student = students[i]

    for(let j = 0; j < 5; j++) {
        // j contols the INNER loop
        let score = student.scores[j]
    }

}