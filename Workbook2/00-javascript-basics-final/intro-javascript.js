var greeting;
greeting = "Welcome to JavaScript";

var firstName, age;

firstNazme = "Natalie"; // "" treated as TEXT (String)
age = 37; // a literal number  -treated as a NUMBER
age = age + 1;

var message = firstName + " is " + age;

console.log(greeting);
console.log(message);

